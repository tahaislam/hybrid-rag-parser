# =============================================================================
# TABLE-AWARE RAG PIPELINE - DEPENDENCIES
# =============================================================================
# PYTHON VERSION REQUIREMENT: 3.9 - 3.11
#
# IMPORTANT: This project requires Python 3.9, 3.10, or 3.11
# Python 3.12+ is NOT supported due to unstructured library constraints
#
# If you have Python 3.12, please install Python 3.11:
#   - Using pyenv: pyenv install 3.11.9 && pyenv local 3.11.9
#   - Using conda: conda create -n rag-pipeline python=3.11
#   - Download from: https://www.python.org/downloads/
# =============================================================================

# Core document parsing
# Let unstructured[pdf] manage its own dependencies (including unstructured-inference)
# Using 0.11.x range as it's stable for Python 3.9-3.11
unstructured[pdf]>=0.11.0,<0.12.0

# PDF processing dependencies
# Flexible versions - let pip resolve
pdf2image
pdfminer.six
pytesseract
pillow

# Table detection and processing (required for hi_res strategy)
python-magic
layoutparser[paddledetection]

# PyTorch - Computer vision for table detection
# Let pip install compatible versions for your Python
torch
torchvision

# Data processing
pandas
numpy

# Future database connectors (to be used in later phases)
# Uncomment when implementing Phase 2:
# pymongo           # MongoDB client for table storage
# qdrant-client     # Qdrant vector database client
# sentence-transformers  # For generating embeddings

# Utilities
python-dotenv
