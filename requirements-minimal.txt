# =============================================================================
# MINIMAL REQUIREMENTS - For Troubleshooting Installation Issues
# =============================================================================
# Use this file if you're having dependency conflicts with requirements.txt
#
# Installation order:
#   pip install --upgrade pip
#   pip install -r requirements-minimal.txt
# =============================================================================

# Step 1: Install unstructured with PDF support FIRST
# This will pull in all its exact dependencies automatically
unstructured[pdf]

# Step 2: Install PyTorch (can be slow, consider CPU-only version)
torch
torchvision

# Step 3: Install table detection
layoutparser[paddledetection]

# Step 4: Install remaining utilities
pandas
numpy
python-dotenv

# =============================================================================
# Alternative: Install one at a time
# =============================================================================
# If the above still fails, install packages individually:
#
#   pip install unstructured[pdf]
#   pip install torch torchvision
#   pip install layoutparser
#   pip install pandas numpy python-dotenv
#
# Then verify:
#   python check_setup.py
# =============================================================================
